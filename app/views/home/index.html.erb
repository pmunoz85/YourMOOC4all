
<div class="col-lg-12 col-md-12 col-sm-12 col-xs-12">
	<% unless @cursos.nil? or @cursos.empty? %>
    <%= render partial: 'cursos/tabla_home', locals: {registros: @cursos} %>
  <% else %>
  	<div class="well">
	  	<h2><%= t('mio.no_encontrado') %></h2>
	  </div>
	<% end %>
</div>




